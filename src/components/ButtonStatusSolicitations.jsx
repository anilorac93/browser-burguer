import React from 'react';

const ButtonStatusSolicitations = ({ onClick }) => {
  return (
    <button className='button-status-solicitations' onClick={onClick}>
      Status pedidos
    </button>
  );
};

export default ButtonStatusSolicitations;