import React from 'react';

const ButtonAbstratctSolicitation = ({ onClick }) => {
  return (
    <button className='button-abstract-solicitation' onClick={onClick}>
      Resumo do pedido
    </button>
  );
};

export default ButtonAbstratctSolicitation;