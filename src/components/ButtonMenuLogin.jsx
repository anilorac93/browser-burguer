import React from 'react';

const ButtonMenuLogin = ({ onClick }) => {
  return (
    <button className= 'menu-login-page' onClick={onClick}>
      Cardápio
    </button>
  );
};

export default ButtonMenuLogin;